/**
 * Test Setup Helper
 * This file contains setup code that runs before all tests.
 * It's imported in vitest.config.ts.
 */

// Set up global test environment variables
process.env.NODE_ENV = 'test';

// Any additional setup needed before tests run
// For example, mocking global objects, setting up test databases, etc.
