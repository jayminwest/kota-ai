{
  "name": "kota-ai",
  "version": "1.0.0",
  "main": "dist/index.js",
  "bin": {
    "kota": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "node dist/index.js",
    "cli": "node dist/cli.js",
    "typecheck": "tsc --noEmit"
  },
  "keywords": ["mcp", "ai", "client"],
  "author": "",
  "license": "ISC",
  "description": "KOTA AI - A client for Model Context Protocol",
  "devDependencies": {
    "@types/node": "^22.14.0",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.8.0"
  }
}
